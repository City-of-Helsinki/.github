name: release-please
on:
  workflow_call:  
    secrets:
      GITHUB_TOKEN:
        required: true
  workflow_dispatch:

permissions:
  contents: write
  pull-requests: write

jobs:
  release-please:
    runs-on: ubuntu-latest
    steps:
      - uses: googleapis/release-please-action@v4
