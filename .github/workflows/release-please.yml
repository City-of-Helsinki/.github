name: release-please
on:
  workflow_call:  
    secrets:
      GITHUB_TOKEN:
        required: true
  workflow_dispatch:

permissions:
  contents: write
  pull-requests: write

jobs:
  release-please:
    runs-on: ubuntu-latest
    steps:
      # hash is version v4.3.0
      - uses: googleapis/release-please-action@c2a5a2bd6a758a0937f1ddb1e8950609867ed15c
